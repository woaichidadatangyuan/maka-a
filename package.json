{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "bin": {
    "maka-cli": "cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "{{author}}",
  "license": "ISC",
  "dependencies": {
    "commander": "^11.1.0",
    "download-git-repo": "^3.0.2"
  }
}
